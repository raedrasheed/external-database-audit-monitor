# EDAM CI pipeline (Epic E7 / EDAM-T051, extended in Epic E6).
name: ci

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  # Build + test on two distinct OS/arch targets.
  build-test:
    name: build-test (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
      - run: npm install --no-audit --no-fund
      - name: Typecheck
        run: npm run typecheck
      - name: Unit + fixture + conformance tests
        run: npm test

  # Lint gate (Epic E6 / mandate E).
  lint:
    name: lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
      - run: npm install --no-audit --no-fund
      - run: npm run lint

  # Coverage gate (Epic E6 / EDAM-T047, mandate D).
  coverage:
    name: coverage
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
      - run: npm install --no-audit --no-fund
      - name: Coverage with thresholds
        run: npm run test:coverage

  # Formal conformance suite (Epic E6 / EDAM-T042..T046, mandate B).
  conformance:
    name: conformance
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
      - run: npm install --no-audit --no-fund
      - name: Run conformance report (C1/C3/C4/C5/C6/C7/C8/C10)
        run: npm run conformance
      - uses: actions/upload-artifact@v4
        with:
          name: conformance-report
          path: conformance-report.json

  # Cross-target determinism rig (Epic E6 / EDAM-T041, mandate A): produce an
  # artifact of envelope_id/event_hash/row_hash + serializer probe per target.
  determinism-rig:
    name: determinism-rig (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
      - run: npm install --no-audit --no-fund
      - name: Build determinism artifact
        run: npx tsx conformance/scripts/determinism-rig.ts > determinism-${{ matrix.os }}.json
      - uses: actions/upload-artifact@v4
        with:
          name: determinism-${{ matrix.os }}
          path: determinism-${{ matrix.os }}.json

  # Gating comparison: ubuntu vs macos must produce identical results+probe.
  determinism-compare:
    name: determinism-compare (GATE)
    runs-on: ubuntu-latest
    needs: determinism-rig
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
      - run: npm install --no-audit --no-fund
      - uses: actions/download-artifact@v4
        with:
          name: determinism-ubuntu-latest
          path: artifacts/ubuntu
      - uses: actions/download-artifact@v4
        with:
          name: determinism-macos-latest
          path: artifacts/macos
      - name: Compare cross-target determinism (fail on any mismatch)
        run: npm run determinism:compare -- artifacts/ubuntu/determinism-ubuntu-latest.json artifacts/macos/determinism-macos-latest.json

  # No-write-path proof (Epic E6 / mandate C; pulls forward EDAM-T052):
  # scan provisioning + runtime source/config for monitored-DB write capability.
  # Comprehensive no-write-path proof (Epic E8 / EDAM-T052): scan source +
  # config + env + compose + deployment + CDC source user for monitored-DB
  # write capability; emit a machine-verifiable proof artifact.
  no-write-proof:
    name: no-write-proof (INV-1)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
      - run: npm install --no-audit --no-fund
      - name: Assert no monitored-DB write path (+ emit proof)
        run: npm run no-write-proof -- --check --report
      - uses: actions/upload-artifact@v4
        with:
          name: no-write-path-proof
          path: no-write-path-proof.json

  # Evidence-schema provenance gate (Sprint-2 / EDAM-T102): the four vendored
  # evidence schemas must match the frozen WORM spec §16 blocks verbatim.
  evidence-schemas-verbatim:
    name: evidence-schemas-verbatim (provenance)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
      - run: npm install --no-audit --no-fund
      - name: Assert vendored evidence schemas match the spec (+ emit proof)
        run: npm run evidence-schemas-verbatim -- --check --report
      - uses: actions/upload-artifact@v4
        with:
          name: evidence-schemas-verbatim-proof
          path: evidence-schemas-verbatim-proof.json

  # WORM no-mutate proof (Sprint-2 / EDAM-T105): Domain-B evidence writer +
  # the WormWriter policy contain no delete/overwrite/retention/key-read path.
  worm-no-mutate-proof:
    name: worm-no-mutate-proof (INV-EV-1)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
      - run: npm install --no-audit --no-fund
      - name: Assert no WORM-mutation path (+ emit proof)
        run: npm run worm-no-mutate-proof -- --check --report
      - uses: actions/upload-artifact@v4
        with:
          name: worm-no-mutate-proof
          path: worm-no-mutate-proof.json

  # Verifier-isolation proof (Epic E2E / EDAM-T147, WV-12 / INV-EV-5).
  verifier-isolation-proof:
    name: verifier-isolation-proof (WV-12)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
      - run: npm install --no-audit --no-fund
      - name: Assert verifier imports stay within the allow-list (+ emit proof)
        run: npm run verifier-isolation-proof -- --check --report
      - uses: actions/upload-artifact@v4
        with:
          name: verifier-isolation-proof
          path: verifier-isolation-proof.json

  # WV-1…WV-14 conformance suite (Epic E2E / EDAM-T148, WV-1…WV-14).
  evidence-conformance:
    name: evidence-conformance (WV-1…WV-14)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
      - run: npm install --no-audit --no-fund
      - name: Assert WV-1…WV-14 against the real implementations (+ emit proof)
        run: npm run evidence-conformance -- --check --report
      - uses: actions/upload-artifact@v4
        with:
          name: evidence-conformance-report
          path: evidence-conformance-report.json

  # Masking & secret-hygiene proof (Epic E8 / EDAM-T053).
  secret-hygiene:
    name: secret-hygiene (INV-2/PII)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
      - run: npm install --no-audit --no-fund
      - name: Assert no secret/PII leakage (+ emit proof)
        run: npm run secret-hygiene -- --check --report
      - uses: actions/upload-artifact@v4
        with:
          name: secret-hygiene-proof
          path: secret-hygiene-proof.json

  # Honesty invariant suite (Epic E8 / EDAM-T054, INV-2).
  invariants:
    name: honesty-invariants (INV-2)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
      - run: npm install --no-audit --no-fund
      - name: Verify honesty invariants (+ emit report)
        run: npm run invariants -- --check
      - uses: actions/upload-artifact@v4
        with:
          name: invariant-report
          path: invariant-report.json

  # Validate the dev Compose stack configuration (no full bring-up).
  compose-validate:
    name: compose-validate
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate docker compose config
        run: docker compose -f deploy/compose/docker-compose.yml config >/dev/null
