{
  "name": "@edam/evidence",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Pure shared evidence determinism keystone (EDAM-T110): segment/manifest/chain-head/segment-head types + canonical manifest assembly, segment_hash, cross-segment continuity, row-chain verification, and canonical anchor-payload assembly. Shared by the evidence writer and the independent verifier. Pure; no I/O; reuses the single canonical serializer.",
  "main": "src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "dependencies": {
    "@edam/canonical": "*",
    "@edam/contracts": "*",
    "@edam/cce-model": "*"
  }
}
