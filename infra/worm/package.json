{
  "name": "@edam/worm",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "WORM evidence store abstraction: role-scoped append-only writer / reader / retention-admin identities (Sprint-2 / EDAM-T103). Interface + in-memory fake; immutable, no overwrite, no delete within retention.",
  "main": "src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "dependencies": {}
}
